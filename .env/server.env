# Read live by bigapp's Next.js server process at request/startup time
# (see bigapp/app/admin/page.tsx) -- NOT baked into the browser bundle, so
# changing these only needs a container restart, never a rebuild.
ADMIN_TITLE=Postmaster
MAIL_HOST=mail.example.com
