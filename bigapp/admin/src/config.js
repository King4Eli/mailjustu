// Set by AdminRoot (app/admin/AdminRoot.tsx) from props a Server Component
// read live from process.env at request time -- see app/admin/page.tsx.
export const config = {
  title: 'Postmaster',
  mailHost: 'mail.example.com',
}
