name: mail_justu

services:
  mail_justu_api:
    build:
      context: ./api
    ports:
      - "4000:80"

  mail_justu_webui:
    build:
      context: ./webUI
    ports:
      - "4001:80"

  mail_justu_admin:
    build:
      context: ./admin
    ports:
      - "4002:80"
