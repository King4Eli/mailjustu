admin@mail.example.com      adminpass123     # super admin (is_admin=1) -- superseded by admin@suyoapp.com below, not yet migrated
jordan@mail.example.com     jordanpass123    # domain admin (is_admin=1) for mail.example.com -- no Services tab, own domain only
newuser@mail.example.com    newuserpass123   # plain mailbox
teammate@mail.example.com   teammatepass1    # plain mailbox, created by jordan via the admin dashboard
admin11@mail.example.com    admin11pass123   # domain admin (is_admin=1) for mail.example.com -- password was unknown, reset via bootstrap-admin.js to log in

## Server super admin (target identity in .env/api.env's SUPER_ADMIN_EMAILS/SUPER_ADMIN_PASSWORD)
## Not created yet -- run `./setup.sh --bootstrap-admin` to activate, then log in and change the password.
admin@suyoapp.com           6ed25d00ee384f50b860da3e
 

# Doveadm HTTP API (dovecot<->api, .env/dovecot.env DOVEADM_PASSWORD --
# kept in sync into .env/api.env by setup.sh, don't edit it there):
767a1898fa53d04b00a63d56ef9f806e9fbc40c4487f070f

# Rspamd controller, .env/api.env RSPAMD_CONTROLLER_PASSWORD -- still the
# unset placeholder; must also be set in
# /etc/rspamd/local.d/worker-controller.inc inside the rspamd_config volume:
change_me
