# Demo / test credentials for this dev stack. Not for production use.
# Regenerate or delete before any real deployment.

## Admin dashboard (http://localhost:5173) -- real login, not a shared token
admin@mail.example.com     adminpass123     # super admin, listed in .env/api.env SUPER_ADMIN_EMAILS -- sees every domain + Services/health
jordan@mail.example.com    jordanpass123    # domain admin (is_admin=1) for mail.example.com only -- no Services tab, own domain only

## Webmail accounts (webUI, http://localhost:5174)
# Any of these can also sign into the admin dashboard above -- what they
# see there depends on their role (super/domain/plain user), same login.
maya@mail.example.com      testpass123
jordan@mail.example.com    jordanpass123    # also a domain admin, see above
newuser@mail.example.com   newuserpass123
teammate@mail.example.com  teammatepass1    # created by jordan via the admin dashboard, to test domain-admin mailbox creation
a@limited.example.com      passpass1        # domain capped at 2 mailboxes / 1 alias each, for testing limits
b@limited.example.com      passpass2

## Aliases (managed by the mailbox owner in webUI -> "Manage aliases")
# jordan@mail.example.com owns: j.smith@mail.example.com, jordan.alt@mail.example.com
#   mail to either lands in jordan's inbox; jordan can also send *as* them.
#   Aliases are restricted to the owner's own domain (mail.example.com) --
#   see api/src/routes/aliases.js.

## Storage quota (real, enforced by Dovecot's quota plugin)
# Default 1024 MB/mailbox (./.env/api.env DEFAULT_MAILBOX_QUOTA_MB), overridable
# per domain via the admin dashboard's Domains tab (quota_mb column).
# Check any account's actual quota: docker exec mail-dovecot doveadm user <email>

## Rspamd controller UI (http://localhost:11334, only when the rspamd
## add-on compose file is included)
# See ./volumes/rspamd/local.d/worker-controller.inc
change_me

## Database (global_mysql, shared instance -- see ./.env/api.env)
# host: global_mysql
# db:   mail_justu
# user: mail_justu_user
# pass: mail_justu_password1

## MySQL root (only relevant if you stand up a dedicated mysql container
## again via ./.env/mysql.env instead of the shared global_mysql)
# root / root_password
# mail (app user) / change_me
